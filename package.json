{
  "name": "stochastix-root",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node index.js"
  }
}
